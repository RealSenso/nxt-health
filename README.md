# MedStartup Hub

A medical-focused startup and entrepreneur support platform with problem statements, funding applications, and category roadmaps.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
